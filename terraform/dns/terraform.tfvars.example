# Copy to terraform.tfvars (gitignored) and fill with real values.
# Better: pass the token via the environment instead of a file:
#   export TF_VAR_cloudflare_api_token=...

cloudflare_api_token = "cf_REPLACE_WITH_REAL_TOKEN"

cloudflare_zone_ids = {
  "example.com" = "0123456789abcdef0123456789abcdef"
  "example.org" = "fedcba9876543210fedcba9876543210"
}

# Record names are FQDNs, exactly as the Cloudflare API returns them.
dns_records = [
  { zone = "example.com", name = "example.com", type = "A", content = "203.0.113.10", proxied = true },
  { zone = "example.com", name = "www.example.com", type = "CNAME", content = "example.com", proxied = true },
  { zone = "example.com", name = "example.com", type = "MX", content = "mail.example.com", priority = 10 },
  { zone = "example.org", name = "api.example.org", type = "A", content = "203.0.113.11" },
]
