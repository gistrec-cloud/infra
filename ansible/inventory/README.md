# inventory

Real inventory lives in `hosts.yml`, which is **gitignored**. Create it from the template:

```bash
cp hosts.yml.example hosts.yml
```

Then fill in your real hostnames / IPs. Nothing in `hosts.yml` is ever committed.
