# log_format для snippet honeypot.conf (access_log в honeypot.log). Должен быть в http{} —
# подключается через include conf.d/*.conf до sites-enabled. Зеркалит РФ-бокс.
log_format honeypot '$remote_addr';
