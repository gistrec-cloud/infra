# honeypot.log lines look like: IP - [time] "METHOD URI"
# (log_format honeypot from the nginx role's honeypot-logformat.conf).
# <HOST> = the leading client IP; the [time] is auto-detected by fail2ban.
[Definition]
failregex = ^<HOST> -
