# {{ ansible_managed }}
# 0600, root-only. Applies only on first init (empty data dir); rotating the root
# password afterwards is a manual ALTER USER — see the role README.
MYSQL_ROOT_PASSWORD={{ mysql_root_password }}
