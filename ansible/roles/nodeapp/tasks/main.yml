---
# Backwards-compatible full role entrypoint. site.yml invokes the two phases
# separately so runtime dependencies exist before registry-managed apps.
- name: Install the Node.js and pm2 runtime
  ansible.builtin.import_tasks: runtime.yml

- name: Deploy legacy node applications
  ansible.builtin.import_tasks: apps.yml
