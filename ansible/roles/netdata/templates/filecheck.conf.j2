# {{ ansible_managed }}
# go.d filecheck — one job per watched file (backup-freshness markers).
jobs:
{% for f in netdata_filecheck_files %}
  - name: {{ f.name }}
    files:
      include:
        - '{{ f.path }}'
{% endfor %}
